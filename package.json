{
  "name": "novaremix-vault",
  "version": "0.8.8",
  "private": true,
  "description": "Novaremix Vault — isolated lending + yield router (internal QA)",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "server": "node scripts/server.mjs",
    "stop": "node scripts/stop.mjs",
    "build": "npm run build -w @novaremix/shared && npm run build -w @novaremix/simulator && npm run build -w @novaremix/console && npm run build -w @novaremix/api",
    "verify": "node scripts/verify.mjs",
    "test": "npm run test -w @novaremix/simulator",
    "test:contracts": "cd contracts && forge test",
    "setup": "node scripts/setup.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
